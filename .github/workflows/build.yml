name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4
        with:
          # The action's default also asks for the legacy `tools` package, which
          # the SDK repository no longer serves — sdkmanager exits 1 on it.
          packages: 'platform-tools'

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Install SDK platform, NDK and CMake
        run: |
          sdkmanager \
            "platforms;android-36" \
            "ndk;27.0.12077973" \
            "cmake;3.22.1"

      - name: Cache Gradle
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon

      # ─── SDL2 must be IN the APK, not merely in the build (convergence plan C1) ────────────────
      #
      # `assembleDebug` going green proves the NDK COMPILED the vendored SDL2. It proves nothing
      # about the artifact, and every one of the three things below is decided somewhere else:
      # whether a .so exists at all is SDL_SHARED, whether it is packaged is AGP, and whether the
      # Java half reaches the DEX is one sourceSet line in app/build.gradle.kts that NOTHING in the
      # app references until phase C3 — so a refactor could delete it and no compiler would say a
      # word. The failure would surface as an inscrutable crash the first time an SDLActivity boots.
      #
      # So the check is on the ARTIFACT, and it names literals only a correct build produces.
      - name: Assert the APK carries SDL2 (convergence C1)
        run: |
          # ⚠️ EVERY check fails EXPLICITLY. An earlier draft of this step relied on `set -e` plus
          # bare `test` lines, and when the llvm-nm lookup silently produced nothing it printed
          # "ALL CHECKS PASSED" over a symbol count of ZERO. A step whose pass is the absence of an
          # error cannot tell a working build from a broken instrument — so nothing here reports a
          # verdict it did not measure, and the evidence is printed either way.
          FAIL=0
          APK=$(ls app/build/outputs/apk/debug/*.apk)
          echo "APK: $APK"

          # (1) the native library, per ABI.
          for ABI in arm64-v8a x86_64; do
            if unzip -l "$APK" | grep -q "lib/$ABI/libSDL2.so"; then
              echo "ok    lib/$ABI/libSDL2.so"
            else
              echo "FAIL  lib/$ABI/libSDL2.so is not in the APK"; FAIL=1
            fi
          done

          # (2) the JNI entry point SDLActivity's static initialiser calls to compare its own
          # hardcoded version triple against the C library's, refusing to start on a mismatch.
          # ⚠️ Read out of the DYNAMIC SYMBOL TABLE, not grepped out of the file: the symbol name
          # also appears in debug info, so a plain `grep libSDL2.so` passes on a library that
          # exports nothing at all.
          NM=$(find "$ANDROID_SDK_ROOT/ndk" -type f -name llvm-nm | head -1)
          if [ -z "$NM" ] || [ ! -x "$NM" ]; then
            echo "FAIL  llvm-nm not found under $ANDROID_SDK_ROOT/ndk - the symbol check cannot run"
            FAIL=1
          else
            unzip -p "$APK" lib/arm64-v8a/libSDL2.so > /tmp/libSDL2.so
            "$NM" -D --defined-only /tmp/libSDL2.so | grep ' T Java_org_libsdl_app_' > /tmp/sdl-jni.txt || true
            COUNT=$(wc -l < /tmp/sdl-jni.txt)
            if grep -q ' T Java_org_libsdl_app_SDLActivity_nativeGetVersion$' /tmp/sdl-jni.txt; then
              echo "ok    nativeGetVersion exported, with $COUNT org.libsdl.app JNI entry points"
            else
              echo "FAIL  nativeGetVersion is not an exported symbol (found $COUNT entry points)"
              FAIL=1
            fi
            [ "$COUNT" -ge 40 ] || { echo "FAIL  only $COUNT JNI entry points exported, expected >= 40"; FAIL=1; }
          fi

          # (3) the Java half, in the DEX.
          FOUND=0
          for D in $(unzip -l "$APK" | grep -o 'classes[0-9]*\.dex'); do
            if unzip -p "$APK" "$D" | grep -aq 'org/libsdl/app/SDLActivity'; then
              echo "ok    org/libsdl/app/SDLActivity in $D"; FOUND=1
            fi
          done
          [ "$FOUND" -eq 1 ] || { echo "FAIL  org/libsdl/app/SDLActivity is in no classes*.dex"; FAIL=1; }

          exit $FAIL

      # ─── pt-ui compiles with the NDK, and is NEVER PACKAGED AS ITS OWN .so ─────────────────────
      #
      # ⚠️ A target being in the CMake model does not mean a build compiles it. AGP fills its ninja
      # command with the targets it can PACKAGE plus their transitive dependencies — so a static
      # archive nothing links is reported BUILD SUCCESSFUL having been compiled zero times, and the
      # independent witness for that is libSDL2main.a, which is never built, while
      # libogg/libopus/libopusfile.a are, because the packaged .so links them.
      #
      # So the target is named explicitly, per ABI, against AGP's OWN configure directory — which is
      # what makes this the shipping toolchain, STL, API level and flags rather than a second
      # configuration free to drift from them. ⚠️ Do not "simplify" it into
      # externalNativeBuild.cmake.targets — that field is a whitelist and would silently stop
      # packaging the first .so somebody forgets.
      #
      # The second half is the invariant: pt-ui is COMPILED and NOT PACKAGED. It reaches the app
      # inside libspritestep-sdl.so, never beside it.
      - name: Compile pt-ui with the shipping NDK configuration
        run: |
          # ⚠️ Explicit per-check failure, and every verdict prints the number it rests on — the C1
          # lesson: a step whose pass is the absence of an error cannot tell a working build from a
          # broken instrument.
          FAIL=0
          NINJA="$ANDROID_SDK_ROOT/cmake/3.22.1/bin/ninja"
          if [ ! -x "$NINJA" ]; then
            echo "FAIL  ninja not found at $NINJA - the compile check cannot run"; exit 1
          fi

          for ABI in arm64-v8a x86_64; do
            # AGP picks the hash in the path, so resolve it — and fail if it is not EXACTLY one.
            # A glob that silently matches nothing is how this step would pass having built nothing,
            # and a stale second directory is how C1 nearly "proved" SDL was not configured at all.
            N=$(ls -d app/.cxx/Debug/*/"$ABI" 2>/dev/null | wc -l)
            if [ "$N" -ne 1 ]; then
              echo "FAIL  expected exactly 1 AGP configure dir for $ABI, found $N"; FAIL=1; continue
            fi
            DIR=$(ls -d app/.cxx/Debug/*/"$ABI")

            if ! "$NINJA" -C "$DIR" pt-ui; then
              echo "FAIL  pt-ui did not compile for $ABI"; FAIL=1; continue
            fi

            # 29 = the source count of the pt-ui target (8 top-level + 21 ui/modules). ⚠️ It is NOT
            # 30: ninja's last edge, "[30/30] Linking CXX static library", is the ARCHIVE, not a
            # translation unit — which is where the A5 note's "all 30 TUs" came from. The first
            # draft of this check asserted >= 30 and would therefore have failed a perfectly good
            # build; it printed "objects=29" beside its own verdict, which is the only reason that
            # was caught rather than debugged later as an Android regression.
            AR="$DIR/libpt-ui.a"
            OBJS=$(find "$DIR/CMakeFiles/pt-ui.dir" -name '*.o' 2>/dev/null | wc -l)
            if [ -f "$AR" ] && [ "$OBJS" -ge 29 ]; then
              echo "ok    $ABI: libpt-ui.a $(stat -c%s "$AR") bytes, from $OBJS objects"
            else
              echo "FAIL  $ABI: libpt-ui.a exists=$([ -f "$AR" ] && echo yes || echo no), objects=$OBJS (expected >= 29)"
              FAIL=1
            fi
          done

          # pt-ui stays a STATIC archive: it reaches the APK linked INSIDE libspritestep-sdl.so
          # (C3), never as a library of its own. A libpt-ui.so appearing would mean someone made the
          # target SHARED and grew every install by a second, dead copy of the UI.
          APK=$(ls app/build/outputs/apk/debug/*.apk)
          if unzip -l "$APK" | grep -q 'libpt-ui\.so'; then
            echo "FAIL  libpt-ui.so is in the APK - pt-ui is linked into the shell .so, not shipped alone"
            FAIL=1
          else
            echo "ok    no libpt-ui.so in the APK (static archive, linked into the shell .so)"
          fi

          exit $FAIL

      - name: Assert the SDL app links pt-ui and is reachable (convergence C3)
        run: |
          # ⚠️ C2's whole finding was that a green build proves nothing about what was COMPILED, and
          # C3's equivalent is that a link line proves nothing about what ENDED UP in the .so. So
          # every check below reads the artifact, and every verdict prints the number it rests on.
          FAIL=0
          NDK="$ANDROID_SDK_ROOT/ndk/27.0.12077973/toolchains/llvm/prebuilt/linux-x86_64/bin"
          NM="$NDK/llvm-nm"
          READELF="$NDK/llvm-readelf"
          for T in "$NM" "$READELF"; do
            if [ ! -x "$T" ]; then echo "FAIL  $T not found - the C3 checks cannot run"; exit 1; fi
          done

          APK=$(ls app/build/outputs/apk/debug/*.apk)

          for ABI in arm64-v8a x86_64; do
            # ── 1. Is it in the APK at all? ──────────────────────────────────────────────────────
            SZ=$(unzip -l "$APK" | awk -v p="lib/$ABI/libspritestep-sdl.so" '$4==p {print $1}')
            if [ -z "$SZ" ]; then
              echo "FAIL  $ABI: libspritestep-sdl.so is NOT in the APK"; FAIL=1; continue
            fi
            echo "ok    $ABI: libspritestep-sdl.so packaged, $SZ bytes"

            # ── 2. SDL_main, in the DYNAMIC symbol table ─────────────────────────────────────────
            # SDLActivity.getMainSharedObject() dlsym's this name out of the last library
            # getLibraries() returns. ⚠️ .dynsym specifically: a plain `grep` of the .so, or a hit in
            # .symtab, would pass on a library that exports nothing — the C1 lesson about reading a
            # symbol out of the debug info and calling it an export.
            unzip -p "$APK" "lib/$ABI/libspritestep-sdl.so" > /tmp/sdl-$ABI.so
            N=$("$READELF" --dyn-syms /tmp/sdl-$ABI.so | grep -cw "SDL_main")
            if [ "$N" -ge 1 ]; then
              echo "ok    $ABI: SDL_main exported in .dynsym ($N entries)"
            else
              echo "FAIL  $ABI: SDL_main NOT in .dynsym - SDLActivity would fail to find the entry point"
              FAIL=1
            fi

            # ── 3. Is pt-ui actually INSIDE it? ──────────────────────────────────────────────────
            # ⚠️ This must read the UNSTRIPPED intermediate, not the APK copy: pt::ui symbols are
            # internal to the .so, so stripping removes them from the packaged artifact. Checking the
            # APK copy here would find zero and mean nothing.
            #
            # ⚠️ And the comparison is against libpt-ui.a's OWN symbol list rather than a hand-picked
            # name. A first draft grepped for "phrase_editor" and friends and got 0 for three of five
            # — because C++ mangled names carry namespaces and classes, not source filenames. A check
            # built on a guessed discriminator fails in both directions.
            D=$(ls -d app/.cxx/Debug/*/"$ABI")
            OBJ=$(ls app/build/intermediates/cxx/Debug/*/obj/"$ABI"/libspritestep-sdl.so 2>/dev/null | head -1)
            if [ ! -f "$OBJ" ]; then
              echo "FAIL  $ABI: no unstripped libspritestep-sdl.so to inspect"; FAIL=1; continue
            fi
            "$NM" --defined-only "$D/libpt-ui.a" 2>/dev/null | grep ' T ' | awk '{print $3}' \
              | grep '^_ZN2pt2ui' | sort -u > /tmp/ptui-$ABI.txt
            "$NM" --defined-only "$OBJ" 2>/dev/null | awk '{print $3}' | sort -u > /tmp/so-$ABI.txt
            TOTAL=$(wc -l < /tmp/ptui-$ABI.txt)
            FOUND=$(comm -12 /tmp/ptui-$ABI.txt /tmp/so-$ABI.txt | wc -l)
            if [ "$TOTAL" -gt 0 ] && [ "$FOUND" -eq "$TOTAL" ]; then
              echo "ok    $ABI: $FOUND of $TOTAL pt::ui symbols are defined in the shell .so"
            else
              echo "FAIL  $ABI: only $FOUND of $TOTAL pt::ui symbols reached the .so (0/0 means the check found nothing - fix the check)"
              FAIL=1
            fi
          done

          # ── 4. The Compose→SDL swap shipped, verified on the merged manifests ──────────────────
          # Through phases C and D the SDL UI rode in a debug-only second activity (SdlActivity,
          # src/debug), and this step asserted it stayed OUT of release. Phase E deleted the Compose
          # MainActivity and moved the SDL activity into src/main UNDER that name, so the invariant
          # inverts: the SDL activity is now the shipping launcher, and the old debug-only one is gone.
          # Verified on the merged manifests rather than believed from the source layout.
          ./gradlew :app:processReleaseManifest :app:processDebugManifest --rerun-tasks --no-daemon
          RELMF=$(ls app/build/intermediates/merged_manifests/release/processReleaseManifest/AndroidManifest.xml)
          DBGMF=$(ls app/build/intermediates/merged_manifests/debug/processDebugManifest/AndroidManifest.xml)
          # The retired debug-only activity must be gone from BOTH manifests.
          RN=$(grep -c 'SdlActivity' "$RELMF" || true)
          DN=$(grep -c 'SdlActivity' "$DBGMF" || true)
          # The shipping launcher must be MainActivity, present in RELEASE.
          MAIN=$(grep -c 'MainActivity' "$RELMF" || true)
          if [ "$RN" -eq 0 ] && [ "$DN" -eq 0 ] && [ "$MAIN" -ge 1 ]; then
            echo "ok    Phase E swap: SdlActivity gone from both manifests, MainActivity is the release launcher ($MAIN)"
          else
            echo "FAIL  Phase E swap: SdlActivity refs debug=$DN release=$RN (want 0/0), MainActivity in release=$MAIN (want >=1)"
            FAIL=1
          fi

          exit $FAIL

      # ⚠️ There is no `./gradlew test` step, and no C++ conformance job either. The sequencer and UI
      # are C++, so the JVM has nothing left to unit-test; the golden regression suite that does cover
      # them lives outside this repository and CI cannot reach it. The JVM recorders that produced
      # those goldens are preserved at tag `kotlin-golden-source`.

      # ─── THE JNI KEEP RULES, IN THE ONLY BUILD THAT CAN BREAK THEM ────────────────────────────
      #
      # ⚠️ **EVERY OTHER CHECK IN THIS JOB READS A DEBUG APK, AND DEBUG NEVER RUNS R8**
      # (`isMinifyEnabled` is release-only). So the SDL DEX assertion above passes by construction —
      # it greps an unminified DEX for a class name — and a keep rule deleted, misspelled or simply
      # not added for a new callback would be invisible here and fatal on a user's device. That is
      # the exact bug class that killed every render in the first v0.9.3 APK.
      #
      # No keystore is needed: `signingConfig` falls back to the debug key when keystore.properties
      # is absent (app/build.gradle.kts), so this builds a genuinely minified APK in CI.
      #
      # ⚠️ The names are extracted FROM proguard-rules.pro, never typed here — a list maintained in
      # two places is a list that will disagree — and the count is asserted before anything is
      # compared, so a parse that quietly matched nothing cannot report success.
      - name: Build release APK and assert the JNI keep rules survived R8
        run: |
          set -u
          ./gradlew assembleRelease --no-daemon
          FAIL=0
          APK=$(ls app/build/outputs/apk/release/*.apk | head -1)
          echo "apk: $APK"

          # The members pinned on MainActivity, taken from the rules file itself.
          awk '/^-keepclassmembers class com\.conanizer\.spritestep\.MainActivity \{/{f=1;next} \
               f&&/^\}/{f=0} f' app/proguard-rules.pro \
            | sed 's/(.*//' | awk '{print $NF}' | grep -v '^$' > /tmp/kept.txt
          WANT=$(wc -l < /tmp/kept.txt)
          echo "keep rules parsed: $WANT"
          [ "$WANT" -ge 13 ] || { echo "FAIL  parsed only $WANT names out of proguard-rules.pro"; exit 1; }

          rm -rf /tmp/dexchk && mkdir -p /tmp/dexchk
          unzip -o -q "$APK" 'classes*.dex' -d /tmp/dexchk

          # ⚠️ ONE grep pattern for BOTH halves. The control below is only evidence that R8 ran in
          # this build if it is measured exactly the way the kept names are.
          present=0
          while read -r n; do
            if cat /tmp/dexchk/classes*.dex | grep -a -q -F "$n"; then
              present=$((present+1))
            else
              echo "FAIL  kept member missing from the release DEX: $n"; FAIL=1
            fi
          done < /tmp/kept.txt
          echo "kept members present: $present of $WANT"

          # THE CONTROL, and without it "13 of 13 present" is equally true of a DEX R8 never touched.
          # These five are private members of the same class with no keep rule; R8 must have renamed
          # every one of them.
          survived=0
          for n in appRoot hideSystemBars importLegacySettings visualizerIndex requestAllFilesAccess; do
            if cat /tmp/dexchk/classes*.dex | grep -a -q -F "$n"; then
              echo "FAIL  (control) non-kept private member survived R8: $n"; survived=$((survived+1)); FAIL=1
            fi
          done
          echo "non-kept members surviving: $survived of 5 (want 0 — proves R8 renamed in this build)"
          exit $FAIL

      - name: Upload APK
        uses: actions/upload-artifact@v7
        with:
          name: spritestep-debug
          path: app/build/outputs/apk/debug/*.apk
          retention-days: 7

  # ─── The SDL shell (convergence plan A4) ────────────────────────────────────────────────────────
  #
  # This job is what keeps the desktop and PortMaster artifacts from being hand-built on one Linux box
  # and never configured anywhere else. Not a hypothetical gap: the first Windows configure ever
  # attempted failed outright, because the audio profiler in sdl-audio-engine.cpp used POSIX
  # clock_gettime, which MSVC does not have — broken for as long as it had existed, with nothing
  # watching.
  #
  # The two runners also cover BOTH branches of shell/CMakeLists.txt's SDL2 lookup, which is worth
  # more than doubling the platform count: ubuntu apt-installs libsdl2-dev, so find_package(SDL2)
  # succeeds and the link is against the SYSTEM SDL — the model every PortMaster CFW requires. Windows
  # has no system SDL, so it falls through to FetchContent and builds SDL from source, which is the
  # model the desktop package (A3) ships. Neither path can rot unnoticed now.
  shell:
    name: SDL shell (${{ matrix.name }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest,  name: linux-x64 }
          - { os: windows-latest, name: windows-x64 }

    steps:
      - uses: actions/checkout@v7

      # System SDL2 on Linux only — see the job comment. Windows deliberately gets none, so that
      # runner exercises the FetchContent fallback.
      - name: Install SDL2 (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libsdl2-dev

      # CMAKE_BUILD_TYPE explicitly, per shell/CMakeLists.txt's own warning: a Debug engine may not
      # keep up with a real-time audio callback, and the usual "default it if empty" guard does not
      # fire on MSVC.
      - name: Configure
        run: cmake -S shell -B shell/build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build shell/build --config Release

      # `error` rather than `warn` on no match — a shell that silently stopped being produced is
      # exactly the failure this job exists to catch, and a green job with an empty artifact would
      # hide it.
      - name: Upload the shell (Linux)
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v7
        with:
          name: spritestep-shell-${{ matrix.name }}
          path: shell/build/spritestep-sdl
          if-no-files-found: error
          retention-days: 7

      # ─── The double-clickable Windows package (convergence plan A3) ──────────────────────────────
      #
      # Windows uploads the ZIP rather than the bare exe, because the zip is the artifact that is
      # meant to reach a person: exe renamed to SPRITESTEP.exe, README, and the licences that a
      # statically linked SDL2 + decoder stack obliges us to ship.
      #
      # The script verifies the ARTIFACT before it zips — that the VC runtime is static (a stranger
      # has no redistributable installed), that the icon in the binary is ours, that the version
      # matches app/build.gradle.kts, and that every statically linked component has a notice — and
      # then reads the whole package back out of the zip. It handles both build layouts itself
      # (ninja puts the exe in the build root, a multi-config VS generator in Release/).
      #
      # ⚠️ `powershell` explicitly, NOT the runner's default shell. It is Windows PowerShell 5.1 the
      # script was written and tested against; pwsh 7 cannot load some of the assemblies it uses, and
      # a package step that only fails on the runner is the exact failure mode this job exists for.
      - name: Package the Windows desktop zip
        if: runner.os == 'Windows'
        shell: powershell
        run: powershell -ExecutionPolicy Bypass -File shell/build-windows.ps1

      - name: Upload the Windows package
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v7
        with:
          name: spritestep-windows-x64
          path: build/windows/*.zip
          if-no-files-found: error
          retention-days: 7

# The A5 "pt-ui NDK canary" job lived here until convergence C2 (2026-07-20). Its purpose was to
# compile pt-ui with the NDK while the target was still gated out of the Android build, so that C2
# would be the un-gating of a target already known to compile rather than the day a pile of platform
# breakage was discovered. It did that job — it compiled clean on the first attempt — and C2 retired
# it, because the `build` job above now compiles pt-ui against AGP's own configure on BOTH shipping
# ABIs. Keeping a second, standalone NDK configuration alongside the real one would have been exactly
# the drift this plan exists to delete. Its option, PT_UI_ON_ANDROID, went with it.
